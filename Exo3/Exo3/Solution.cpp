/*
std::vector<int> Gen_12(int num_cases) {
	std::vector<int> ret(num_cases);

	for (int i = 0; i < ret.size(); i++) {
		ret[i] = 12;
	};
	return ret;
}

std::vector<std::vector<int>> Gen_Matrice(int num, int carre) {
	std::vector<std::vector<int>> ret(carre);

	for (int i = 0; i < ret.size(); i++) {
		for (int j = 0; j < ret.size(); j++) {
			ret[i].push_back(num);
		};
	};
	return ret;
}
*/